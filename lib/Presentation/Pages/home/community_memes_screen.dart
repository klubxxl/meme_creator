import 'package:flutter/material.dart';

class CommunityMemesPage extends StatelessWidget {
  const CommunityMemesPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Another users memes'),
    );
  }
}
